const express = require('express');
const app = express();

const mongoose = require('mongoose');
const ObjectId = mongoose.Types.ObjectId;

const studyController = {

    /** 스터디 정보 조회 */
    async getStudyInfo(req, res, next) {
        try {

        } catch {

        }
    }

}

module.exports = studyController;
