const express = require('express');
const app = express();
const mongoose = require('mongoose');
const ObjectId = mongoose.Types.ObjectId;

const studyRepository = {

    /** 스터디 정보 조회 */
    async findStudyId(study_id) {
        
    }

}

module.exports = studyRepository;